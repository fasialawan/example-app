node {
	def app

	stage{'clone repository'}{
		checkout scm
	}

	stage('Build image'){
		app = docker.build('faisalawan/example-app')
	}

	stage('Push image'){
		docker.withRegistry('https://registry.hub.docker.com', 'docker-hub-credentials'){
			app.push('latest')
		}	
	}
}

